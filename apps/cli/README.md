# DSH Console

`dsh-console` is a TypeScript and React/Ink terminal frontend for DeepSeek Harness.

```sh
npx @cofy-x/dsh-console --prompt "hello"
```

The package installs the `dsh-console` command and includes its standard DSH
plugin bundle.
